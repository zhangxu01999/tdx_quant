__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
# 行情和派生 Parquet 作为跨设备研究快照纳入版本管理。
# 注意：每次同步数据后都需要提交并推送 tdx-quant，再更新主项目子模块指针。
.DS_Store
data/.daily-sync-checkpoint.json

# frontend: generated by data_export.py from data/*.parquet (regenerable)
frontend/assets/*.json

# Claude Code MCP 配置（含 TDX_API_KEY，勿提交）
.mcp.json
