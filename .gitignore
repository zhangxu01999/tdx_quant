__pycache__/
*.py[cod]
.pytest_cache/
.idea/*
!.idea/runConfigurations/
!.idea/runConfigurations/*.xml
.venv/
venv/
# 行情和派生 Parquet 作为跨设备研究快照纳入版本管理。
# 注意：每次同步数据后都需要提交并推送 tdx-quant，再更新主项目子模块指针。
.DS_Store
data/.daily-sync-checkpoint.json
data/.auction-history-checkpoints/
data/auction-history-sync-report.json
data/auction-history-smoke-report.json
data/auction_history_daily_smoke/
# 盘中模拟数据库持续增长且包含本机运行状态，不作为跨设备历史行情快照提交。
data/*.duckdb
data/*.duckdb.wal

# frontend: generated by data_export.py from data/*.parquet (regenerable)
frontend/assets/*.json

# Claude Code MCP 配置（含 TDX_API_KEY，勿提交）
.mcp.json
